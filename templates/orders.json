[ {% for order in orders %}
        {
        "check_in": "{{order.check_in}}", 
        "check_out": "{{order.check_out}}", 
        "status": "{{order.status}}", 
        "hotel": "{{order.room.hotel}}",
        "address" : "{{order.room.hotel.address}}",
        "room_type": "{{order.room.room_type}}",
        "price" : "{{order.price}}",
        "location" : "{{order.room.hotel.location}}"
        }, {% endfor %}
]
